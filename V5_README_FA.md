# املاک ملکی V5

رابط Godot اکنون شامل ورود/ثبت‌نام، پروفایل، ثبت آگهی، جستجو، فیلتر فروش/اجاره و جزئیات آگهی است و به API V4 متصل می‌شود.

## نکته اتصال
برای Android Emulator مقدار `10.0.2.2:3000` مناسب است. برای گوشی واقعی باید `API_BASE` در `godot/scripts/main.gd` به دامنه HTTPS سرور تغییر کند.

## عکس آگهی
Backend آپلود تصویر دارد؛ اتصال انتخاب فایل از داخل Godot در مرحله بعدی باید با FileDialog/Android permissions و multipart تکمیل و روی دستگاه واقعی تست شود.

## APK
این بسته سورس آماده Godot است، نه APK تست‌شده. ساخت APK به Android SDK/Gradle و تست روی گوشی نیاز دارد.
